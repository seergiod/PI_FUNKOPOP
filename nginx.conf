events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name tu-dominio.com www.tu-dominio.com; # Cambia esto por tu dominio o IP de AWS

        # Web normal abierta a todo el mundo
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ =404;
        }

        # SECCIÓN PRIVADA: Carpeta de descargas protegida
        location /descargas {
            root /usr/share/nginx/html;
            auth_basic "Zona Protegida - Solo Amigos";
            auth_basic_user_file /etc/nginx/.htpasswd; # Archivo dentro del contenedor
            
            autoindex on; 
        }
    }
}